const http=require('http'),fs=require('fs'),path=require('path'),root=path.resolve(__dirname,'..');http.createServer((q,s)=>{let f=path.join(root,decodeURIComponent(q.url==='/'?'/index.html':q.url));fs.readFile(f,(e,d)=>{s.writeHead(e?404:200,{'Content-Type':f.endsWith('.js')?'text/javascript':'text/html'});s.end(e?'Not found':d)})}).listen(8080,()=>console.log('http://localhost:8080'));
